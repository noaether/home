{
  "name": "home",
  "version": "1.0.0",
  "description": "noae.ca hopefully",
  "source": "src/parcel.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/**.html ",
    "build": "parcel build src/**.html src/posts/*",
    "prettier": "prettier --write \"src/**/*\"",
    "deploy": "rm -rf ./dist/ && npm run prettier && npm run build && netlify deploy --prod ./dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/noaether/home.git"
  },
  "author": "noaether",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/noaether/home/issues"
  },
  "homepage": "https://github.com/noaether/home#readme",
  "devDependencies": {
    "@parcel/compressor-brotli": "^2.8.3",
    "@parcel/compressor-gzip": "^2.8.3",
    "markdown-it-highlightjs": "^4.0.1",
    "parcel": "^2.8.3",
    "prettier": "^2.8.4"
  }
}
